<?php

class Message extends CI_Model
{
/*

    //内容
    public $content;

    //添加时间
    public $add_time;

    //管理员id
    public $manager_id;

    public function __construct()
    {

        $this->add_time = time();

        $this->manager_id = MANAGER_ID;
    }


    public function append($receiver_id,$content)
    {
        if(is_array($receiver_id)){
            $insert_data = array();
            foreach($receiver_id as $v){
                $data = array(
                    'sender_id' => $this->manager_id,
                    'receiver_id' => $v,
                    'add_time' => $this->add_time,
                    'content' => addslashes($content),
                    'viewed'=>0
                );
                array_push($insert_data,$data);
            }
            $this->db->insert_batch('message', $insert_data);
        }else{
            $insert_data = array(
                'sender_id' => $this->manager_id,
                'receiver_id' => $receiver_id,
                'add_time' => $this->add_time,
                'content' => addslashes($content),
                'viewed'=>0
            );
            $this->db->insert('message', $insert_data);
        }
    }
*/
}

?>